# ConsenTide - GDPR Consent Dynamic Ledger

**Zero-knowledge GDPR consent management with immutable audit trails and dynamic revocation capabilities**

## 🎯 Overview

ConsenTide is a privacy-first consent ledger that lets users grant, monitor, and revoke data-processing permissions across any organization—without exposing personal data. Built on Constellation's Hypergraph for true immutability and Metagraphs for custom compliance logic.

## 🏗️ Architecture

### System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                                │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │ User Dashboard   │  │ Controller Portal│  │ Regulator Console│  │
│  │ (Next.js SSR)    │  │ (Next.js SSR)    │  │ (Next.js SSR)    │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                     │                      │            │
│           └─────────────────────┴──────────────────────┘            │
│                                 │                                   │
└─────────────────────────────────┼───────────────────────────────────┘
                                  │ REST API (JWT Auth)
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        API GATEWAY LAYER                            │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Express.js API Gateway (Port 3001)                           │  │
│  │  ├─ Authentication Middleware (JWT Validation)                │  │
│  │  ├─ Authorization Middleware (RBAC)                           │  │
│  │  ├─ Rate Limiting & Security (Helmet.js)                      │  │
│  │  └─ CORS Policy Enforcement                                   │  │
│  └───────────────────────────────────────────────────────────────┘  │
│           │                     │                      │            │
│      ┌────▼────┐          ┌────▼────┐           ┌────▼────┐        │
│      │ /auth   │          │/consent │           │/governance│       │
│      │ Routes  │          │ Routes  │           │  Routes   │       │
│      └────┬────┘          └────┬────┘           └────┬────┘        │
└───────────┼────────────────────┼──────────────────────┼─────────────┘
            │                    │                      │
            ▼                    ▼                      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ Auth Service │  │Consent Service│  │Governance Svc│              │
│  │ (Native PG)  │  │  (Native PG)  │  │  (Native PG) │              │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘              │
│         │                 │                  │                      │
│         │    ┌────────────▼──────────┐       │                      │
│         │    │  Blockchain Facade    │       │                      │
│         │    │  ┌─────────────────┐  │       │                      │
│         │    │  │ MockHGTPService │◄─┼───────┘ (Demo Mode)         │
│         │    │  │ (Active)        │  │                              │
│         │    │  └─────────────────┘  │                              │
│         │    │  ┌─────────────────┐  │                              │
│         │    │  │DigitalEvidence  │  │                              │
│         │    │  │Service (Planned)│  │ (Future: Real Blockchain)   │
│         │    │  └─────────────────┘  │                              │
│         │    └───────────────────────┘                              │
│         │                 │                                          │
└─────────┼─────────────────┼──────────────────────────────────────────┘
          │                 │
          ▼                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  PostgreSQL Database (Replit Native)                          │  │
│  │  ├─ users (DID, public keys, roles)                           │  │
│  │  ├─ auth_credentials (bcrypt hashed passwords)                │  │
│  │  ├─ consents (status, HGTP TX hash, timestamps)               │  │
│  │  ├─ controllers (organization registry)                       │  │
│  │  ├─ governance_proposals (El Paca voting)                     │  │
│  │  └─ audit_logs (complete consent lifecycle)                   │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                  BLOCKCHAIN LAYER (Future)                          │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Constellation Network (HGTP)                                 │  │
│  │  ├─ Digital Evidence API (Integration Ready)                  │  │
│  │  ├─ L0 Metagraph Node (Custom Deployment Option)              │  │
│  │  └─ L1 DAG (Immutable Audit Trail)                            │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### Blockchain Integration Architecture

```
┌────────────────────────────────────────────────────────────────┐
│              CURRENT STATE: DEMO MODE                          │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  pgConsentService.grantConsent()                               │
└───────────────┬────────────────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────┐
│  BlockchainFacade.anchorConsent()                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if (DIGITAL_EVIDENCE_API_KEY exists)                     │  │
│  │   → return new DigitalEvidenceService()  ❌ NOT IMPL     │  │
│  │ else                                                      │  │
│  │   → return mockHGTPService  ✅ ACTIVE                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────┬────────────────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────┐
│  MockHGTPService.anchorConsent()                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. Generate deterministic hash:                          │  │
│  │    mock::grant_consent::sha256(consentData)              │  │
│  │ 2. Create mock block height: Date.now() / 60000          │  │
│  │ 3. Create mock merkle root: sha256(consentId + hash)     │  │
│  │ 4. Return BlockchainReceipt with mock TX hash            │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────┬────────────────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────┐
│  PostgreSQL: INSERT INTO consents (hgtp_tx_hash = mock::...)  │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│              FUTURE STATE: REAL BLOCKCHAIN                     │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│  BlockchainFacade.anchorConsent()                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if (DIGITAL_EVIDENCE_API_KEY exists)                     │  │
│  │   → return new DigitalEvidenceService()  ✅ IMPLEMENTED  │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────┬────────────────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────┐
│  DigitalEvidenceService.anchorConsent()                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 1. POST to Digital Evidence API                          │  │
│  │ 2. Sign with Ed25519 (Constellation credentials)         │  │
│  │ 3. Submit to L1 DAG                                      │  │
│  │ 4. Return real TX hash from Constellation Network        │  │
│  └──────────────────────────────────────────────────────────┘  │
└───────────────┬────────────────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────┐
│  Constellation Network L1 DAG (Immutable Record)               │
└────────────────────────────────────────────────────────────────┘
```

### Consent Lifecycle Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER GRANTS CONSENT                          │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  POST /api/v1/consent/grant                                    │
│  Body: {                                                       │
│    purpose: "marketing",                                       │
│    controller_hash: "org123",                                  │
│    data_categories: ["email", "name"]                          │
│  }                                                              │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  JWT Authentication Middleware                                 │
│  ├─ Extract user_id from JWT token                             │
│  ├─ Validate token signature                                   │
│  └─ Check token expiration                                     │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  pgConsentService.grantConsent()                               │
│  1. Generate consent_id (UUID)                                 │
│  2. Create ZK proof (placeholder for demo)                     │
│  3. Anchor to blockchain via BlockchainFacade                  │
│     └─> MockHGTPService returns mock TX hash                   │
│  4. INSERT INTO consents (status='granted', hgtp_tx_hash)      │
│  5. INSERT INTO audit_logs (action='grant_consent')            │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Response: {                                                   │
│    consent_id: "uuid-1234",                                    │
│    status: "granted",                                          │
│    hgtp_tx_hash: "mock::grant_consent::abc123...",            │
│    block_height: 1234567,                                      │
│    merkle_root: "def456..."                                    │
│  }                                                              │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Frontend Dashboard Updates                                    │
│  ├─ Display consent card with "Active" status                  │
│  ├─ Show mock blockchain TX hash                               │
│  └─ Enable "Revoke" button                                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    USER REVOKES CONSENT                         │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  POST /api/v1/consent/revoke                                   │
│  Body: { consent_id: "uuid-1234" }                             │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  pgConsentService.revokeConsent()                              │
│  1. SELECT consent WHERE consent_id AND user_id (ownership)    │
│  2. Anchor revocation via BlockchainFacade                     │
│     └─> MockHGTPService.updateConsentStatus()                  │
│         └─> Returns mock::revoke_consent::xyz789...            │
│  3. UPDATE consents SET status='revoked', revoked_at=NOW()     │
│  4. INSERT INTO audit_logs (action='revoke_consent')           │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Response: { status: "revoked", tx_hash: "mock::revoke..." }  │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Frontend Dashboard Updates                                    │
│  ├─ Update consent card status to "Revoked"                    │
│  ├─ Disable data processing for this consent                   │
│  └─ Show revocation timestamp                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Authentication & Authorization Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER REGISTRATION                            │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  POST /api/v1/auth/register                                    │
│  Body: {                                                       │
│    email: "user@example.com",                                  │
│    password: "securepass123",                                  │
│    full_name: "John Doe"                                       │
│  }                                                              │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  pgAuthService.registerUser()                                  │
│  1. Validate email format                                      │
│  2. Check if email already exists                              │
│  3. Generate DID (Decentralized Identifier)                    │
│  4. Generate Ed25519 key pair (public/private)                 │
│  5. Hash password with bcrypt (10 rounds)                      │
│  6. INSERT INTO users (did, public_key, role='user')           │
│  7. INSERT INTO auth_credentials (password_hash)               │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Response: {                                                   │
│    user_id: "uuid-5678",                                       │
│    did: "did:constellation:abc123",                            │
│    message: "Registration successful"                          │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       USER LOGIN                                │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  POST /api/v1/auth/login                                       │
│  Body: { email: "user@example.com", password: "securepass" }  │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  pgAuthService.loginUser()                                     │
│  1. SELECT user JOIN auth_credentials WHERE email              │
│  2. Verify password with bcrypt.compare()                      │
│  3. Generate JWT token with payload:                           │
│     {                                                           │
│       user_id: "uuid-5678",                                    │
│       email: "user@example.com",                               │
│       role: "user",                                            │
│       did: "did:constellation:abc123"                          │
│     }                                                           │
│  4. Sign with JWT_SECRET (expires in 24h)                      │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Response: {                                                   │
│    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",           │
│    user: { id, email, role, did }                              │
│  }                                                              │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Frontend stores token in localStorage                         │
│  All subsequent API calls include header:                      │
│  Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI...          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  PROTECTED API REQUEST                          │
└────────────┬────────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  GET /api/v1/consent/list                                      │
│  Headers: { Authorization: "Bearer <token>" }                  │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Authentication Middleware                                     │
│  1. Extract token from Authorization header                    │
│  2. Verify token signature with JWT_SECRET                     │
│  3. Check token expiration                                     │
│  4. Decode payload → attach req.user = decoded                 │
│  5. If invalid → Return 401 Unauthorized                       │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Authorization Middleware (RBAC)                               │
│  1. Check req.user.role                                        │
│  2. Validate role permissions for this endpoint                │
│  3. If role='controller' → only access own organizationId      │
│  4. If unauthorized → Return 403 Forbidden                     │
└────────────┬───────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────────────────────┐
│  Route Handler Executes                                        │
│  Access to req.user.user_id, req.user.role, etc.              │
└─────────────────────────────────────────────────────────────────┘
```

## 🚀 Features

- 🎭 **Blockchain Anchoring** – Mock/fallback system for demo (Digital Evidence API integration ready)
- ✅ **Dynamic Consent Lifecycle** – Grant → Use → Revoke → Audit in real-time
- ✅ **Cross-Platform Integration** – RESTful API for any system (CRM, ERP, marketing tools)
- ✅ **Regulatory Compliance Dashboard** – Real-time GDPR Article 7 & 13 compliance status
- ✅ **Immutable Audit Trail** – Every consent action hash-anchored (mock for demo)
- ✅ **Token-Governed Privacy** – El Paca used for community voting on privacy policies
- ✅ **Production Database** – Native Replit PostgreSQL with pg client
- ✅ **Authentication** – Email/password + JWT tokens (native implementation)
- ✅ **GDPR Compliant Schema** – Complete database schema with proper indexing
- 🎭 **Deterministic Mock Blockchain** – Consistent TX hashes for all stakeholder actions
- 🚧 **Zero-Knowledge Proofs** – Placeholder implementation (full circuits pending)

## 📁 Project Structure

```
Consentire/
├── frontend/          # Next.js React dashboard (User + Admin)
├── backend/           # Node.js Express API Gateway
├── metagraph/         # Scala L0 Metagraph implementation
├── database/          # PostgreSQL schema & migrations
├── shared/           # Shared TypeScript types & validation
├── scripts/          # Deployment & setup scripts
└── docs/             # Comprehensive documentation
```

## 🚀 Quick Start

See [QUICKSTART.md](./QUICKSTART.md) for a 5-minute setup guide!

## 🛠️ Development

### Prerequisites

- Node.js 18+
- npm or yarn
- Scala 2.13+ (for Metagraph)
- sbt 1.8+ (for Metagraph)

### Setup

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Build shared types:**
   ```bash
   cd shared && npm run build && cd ..
   ```

3. **Run backend API:**
   ```bash
   cd backend && npm run dev
   ```

4. **Run frontend (in new terminal):**
   ```bash
   cd frontend && npm run dev
   ```

5. **Build Metagraph (optional):**
   ```bash
   cd metagraph && sbt compile
   ```

For detailed setup instructions, see [docs/SETUP.md](./docs/SETUP.md).

## 🚀 Production Deployment

### Replit Deployment (Current)
The application is **already deployed and running** on Replit with:
- Frontend: Port 5000 (Next.js)
- Backend: Port 3001 (Express API)
- Database: Managed PostgreSQL
- Constellation: Mainnet credentials in Replit Secrets

### Environment Configuration
See `.env.example` for required environment variables. Critical credentials (Constellation private keys) must be stored in **Replit Secrets**, not in code.

## 🗄️ Database Setup

### Replit PostgreSQL (Native)
1. **Database automatically provisioned** by Replit
2. **Environment variables auto-configured** (DATABASE_URL, PGHOST, PGPORT, PGUSER, PGPASSWORD)
3. **Schema**: SQL schema in `database/schema.sql`
4. **Data Access**: Native PostgreSQL services using `pg` library
5. **Production-ready** with automatic backups

### Manual Schema Setup
```bash
# Run schema manually if needed
PGPASSWORD=$PGPASSWORD psql -h $PGHOST -U $PGUSER -p $PGPORT -d $PGDATABASE -f database/schema.sql
```

## ⛓️ Blockchain Integration

### Current Status: DEMO MODE (Mock Blockchain)

The system currently runs in **DEMO MODE** with simulated blockchain anchoring while awaiting Digital Evidence API credentials.

#### Why Mock Mode?

Constellation Network's `/data` endpoint (for custom application data) is only available on **custom deployed metagraphs**, not the public mainnet. We have two production-ready paths:

**Option 1: Digital Evidence API** ⭐ **RECOMMENDED**
- Public API by Constellation Network
- Designed for tamper-proof, court-admissible records
- Perfect fit for GDPR consent audit trails
- Feeless blockchain anchoring
- **Status**: Waiting for API key registration

**Option 2: Deploy Custom Metagraph**
- Full control over data layer
- Requires infrastructure: 3+ EC2 instances, Scala compilation, node deployment
- Setup time: 1-2 days + AWS costs
- **Status**: Code ready in `/metagraph` folder (not actively used)

#### Mock System Architecture

```typescript
// Blockchain Service Abstraction Layer
IBlockchainAnchorService
├── RealHGTPService (production - awaits credentials)
├── MockHGTPService (demo - deterministic simulation)
└── BlockchainFacade (factory - auto-selects based on env)
```

**Mock Features:**
- ✅ Deterministic TX hash generation (consistent across sessions)
- ✅ Realistic block heights and merkle roots
- ✅ Full support for all stakeholder actions:
  - User: Grant consent, Revoke consent, Modify consent
  - Controller: Verify consent permissions
  - Regulator: Audit queries
- ✅ Complete audit trail in PostgreSQL
- ✅ UI indicator showing "DEMO MODE" status
- ✅ **Architecture ready** for real blockchain integration (adapter implementation pending)

**Mock TX Hash Format:**
```
mock::grant_consent::a1b2c3d4e5f6...
mock::revoke_consent::x9y8z7w6v5...
mock::controller_verify::m4n3o2p1...
```

#### Switching to Real Blockchain (Integration Ready - Not Yet Implemented)

**Architecture is ready** for seamless integration, but requires implementing the Digital Evidence API adapter:

**Step 1: Obtain Digital Evidence API Key**
```bash
# Register at Constellation Network
# Get API key from: https://de-api.constellationnetwork.io
```

**Step 2: Implement DigitalEvidenceService**
```typescript
// Create backend/src/services/digitalEvidenceService.ts
// Implement IBlockchainAnchorService interface
// Use Digital Evidence API for real blockchain anchoring
```

**Step 3: Update BlockchainFacade**
```typescript
// Uncomment real adapter instantiation in blockchainFacade.ts
if (hasDigitalEvidenceKey) {
  this.service = new DigitalEvidenceService();
  this.isDemoMode = false;
}
```

**Step 4: Configure Environment**
```bash
# Add to Replit Secrets
DIGITAL_EVIDENCE_API_KEY=your_api_key_here
```

**Result:** Automatic switch from mock to real blockchain, no changes to business logic required.

#### API Endpoint for Status

```bash
GET /api/v1/status
```

Response:
```json
{
  "success": true,
  "blockchain": {
    "mode": "mock",
    "isDemo": true,
    "message": "Running in DEMO mode - configure Digital Evidence API credentials to enable real blockchain anchoring",
    "networkStatus": { ... }
  },
  "database": {
    "connected": true,
    "type": "PostgreSQL"
  }
}
```

#### Frontend Demo Mode Indicator

Yellow banner appears at top of all dashboards when in demo mode:

```
⚠️ DEMO MODE: Blockchain operations are simulated for demonstration purposes.
   Configure Digital Evidence API credentials to enable real blockchain anchoring.
```

## 🔐 Authentication

### Supported Methods
- **Email/Password**: Native authentication with bcrypt hashing (10 salt rounds)
- **JWT Tokens**: Server-issued tokens with configurable expiration (24-hour default)
- **Role-Based Access Control**: User, Controller, Admin, Regulator roles
- **Session Management**: Stateless JWT authentication

### Demo Accounts
- **Admin**: `admin@consentire.io` / `admin123`
- **User**: `user@consentire.io` / `user123`
- **Organization**: `org@consentire.io` / `org123`
- **Regulator**: `regulator@consentire.io` / `reg123`

## 📖 API Documentation

See [docs/API.md](./docs/API.md) for full API documentation.

## 🏆 Current Status

### ✅ Production Ready (Deployed on Replit)
- **Database Integration**: Native Replit PostgreSQL with direct pg client
  - SQL schema in `database/schema.sql`
  - Native PostgreSQL services (pgConsentService, pgControllerService, pgGovernanceService)
  - No external dependencies - pure PostgreSQL queries
- **Authentication System**: JWT-based authentication with bcrypt password hashing
  - Email/password authentication  
  - Role-based access control (User, Controller, Admin, Regulator)
- **User Interface**: Complete dashboard and admin console (Next.js 14)
  - Demo mode indicator banner for transparency
  - Real-time blockchain status display
- **API Gateway**: RESTful endpoints for all operations (Express + TypeScript)
  - System status endpoint (`/api/v1/status`)
  - Blockchain mode reporting
- **GDPR Compliance**: Complete schema with proper indexing and audit trails
- **Blockchain Architecture**: Production-ready abstraction layer
  - **Current Mode**: DEMO (Mock blockchain with deterministic TX hashes)
  - **Interface**: `IBlockchainAnchorService` for seamless real/mock switching
  - **Mock Service**: Full support for all stakeholder actions (grant, revoke, verify, audit)
  - **Real Service**: Ready for Constellation Digital Evidence API integration
  - **Fallback**: Automatic mode detection based on environment credentials
- **Production Deployment**: Running on Replit with production credentials in Secrets

### 🎭 Demo Mode (Current)
- **Blockchain Operations**: Simulated with deterministic, traceable TX hashes
  - Mock TX format: `mock::action::hash`
  - Consistent hashing for reproducibility
  - Full audit trail in PostgreSQL
- **Stakeholder Actions Covered**:
  - ✅ User: Grant, revoke, modify consent
  - ✅ Controller: Verify consent permissions
  - ✅ Regulator: Audit queries and compliance checks
- **UI Transparency**: Yellow banner indicating demo mode
- **API Status**: `/api/v1/status` endpoint reports current blockchain mode

### 🚀 Ready for Production Blockchain (Architecture Only)
- **Digital Evidence API Integration**: Abstraction layer complete, adapter not yet implemented
  - `IBlockchainAnchorService` interface defined
  - `BlockchainFacade` factory pattern ready
  - Requires implementing `DigitalEvidenceService` class
  - Estimated implementation time: 2-4 hours
- **Alternative Path**: Custom metagraph deployment (code in `/metagraph`, requires infrastructure)

### 🚧 In Development
- **ZK Proofs**: Placeholder mode (full circom circuit compilation pending)
  - Service implemented but using simulated proofs
  - Production requires: `circom consent.circom --r1cs --wasm --sym` + snarkjs setup

### 🎯 Hackathon Demo Ready
- **Working MVP**: End-to-end consent management with mock blockchain anchoring
- **Production Database**: Replit PostgreSQL with complete GDPR schema
- **Demo Blockchain**: Deterministic simulation for all consent operations
- **Scalable Architecture**: Modular, maintainable TypeScript codebase with abstraction layers
- **Complete Documentation**: Setup guides, API docs, blockchain integration roadmap
- **Transparent Demo Mode**: Clear indicators to evaluators about mock vs real blockchain

## 🔒 Security

- Zero-knowledge proofs (placeholder - full circuits pending)
- SHA-256 hashing for controller identifiers
- Ed25519 signatures for Constellation transactions
- Immutable HGTP anchoring to mainnet
- Bcrypt password hashing (10 salt rounds)
- JWT token-based authentication

## 📄 License

MIT License - See [LICENSE](./LICENSE) for details.

## 🤝 Contributing

This project is built for the Constellation x LegalTech Hackathon. Contributions welcome!

## 📞 Contact

- GitHub: [ConsenTide](https://github.com/consentire)
- Discord: Constellation Community
